requests
pygit2>=1.12.0
rich>=13.0.0
click>=8.0.0

# CLI y argumentos
typer[all]

# LLMs
openai

# Tipado y validación
pydantic
pydantic-settings

# Para ejecutar procesos del sistema (ya está en stdlib, pero por si usas alguna wrapper en el futuro)

# Para cargar archivos JSON o config si lo deseas
python-dotenv  # Opcional para futuras configuraciones .env

# Si más adelante usas modelos locales vía LlamaCpp, HuggingFace, etc., puedes incluir:
# llama-cpp-python
# transformers
